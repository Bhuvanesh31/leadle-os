# Letter grades from fixed thresholds (v1). v2 replaces with portfolio percentiles.
# Each metric: descending list of [min_fraction, letter]; first match wins.
grades:
  open_rate:   [[0.20, "A"], [0.12, "B"], [0.08, "C"], [0.04, "D"], [0.0, "F"]]
  reply_rate:  [[0.07, "A"], [0.04, "B"], [0.02, "C"], [0.01, "D"], [0.0, "F"]]
  positive:    [[0.03, "A"], [0.015, "B"], [0.007, "C"], [0.003, "D"], [0.0, "F"]]
  bounce_rate: [[0.0, "A"], [0.02, "B"], [0.04, "C"], [0.06, "D"], [0.08, "F"]]   # lower is better (ascending)
  accept_rate: [[0.30, "A"], [0.20, "B"], [0.12, "C"], [0.05, "D"], [0.0, "F"]]
# bounce_rate is "lower is better": compute applies it ascending (see compute.grade()).
ascending_metrics: ["bounce_rate"]
# Tracker status -> outcome classification (case-insensitive substring match).
positive_statuses: ["interested", "positive", "follow up", "long follow up", "meeting", "call booked"]
meeting_statuses: ["meeting", "call booked", "demo"]
# Engagement-timing heatmap.
timezone: "America/New_York"   # UPSTA primary region; SG events fold in with a caveat note
dayparts:
  - ["Early 5-9",     5,  9]
  - ["Morning 9-12",  9,  12]
  - ["Midday 12-15",  12, 15]
  - ["Afternoon 15-18", 15, 18]
  - ["Evening 18-22", 18, 22]
toggles:
  show_data_quality_notes: true
bounce_flag_threshold: 0.04   # senders at/above this bounce fraction get flagged
# Industry benchmarks for the scorecard reference line.
benchmarks:
  open_rate: 0.20
  click_rate: 0.02
  positive_replies_month: 4
  total_replies_month: 12
  bounce_rate_max: 0.04
